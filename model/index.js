const User = require('./User');
const Thought = require('./Thought');

//we import all the models in the folder then export them as an object so we can use them in the routes

module.exports = { User, Thought };   